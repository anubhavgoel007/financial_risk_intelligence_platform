"""Presentation layer (Interface Adapters).

Translates HTTP requests into application use case calls and formats
responses. Contains FastAPI routers, Pydantic request/response schemas,
and dependency providers. Depends on the application layer only.
"""
