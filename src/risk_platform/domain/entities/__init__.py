"""Core business entities (e.g. Client, Loan, RiskProfile, Transaction).

Expressed as plain Python objects/dataclasses with identity and invariants,
independent of any persistence or API concern.
"""
